node_modules
dist
dev-dist

# Local TURN credentials (see .env.example)
.env
.env.local
.env.*.local
# Wrangler's local secrets file — holds the real TURN key and API token
.dev.vars
.dev.vars.*

# Wrangler local state
.wrangler
.DS_Store
*.log

# Large local-only media (real content). Regenerate from source with ffmpeg — see README.
# The small synthetic test clip in src/content/ IS committed (offline default).
public/media/

# Yarn 4 (node-modules linker)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
