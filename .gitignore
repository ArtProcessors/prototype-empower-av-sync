node_modules
dist
dev-dist

# Local TURN credentials (see .env.example)
.env
.env.local
.env.*.local
# Wrangler's local secrets file — holds the real TURN key and API token
.dev.vars
.dev.vars.*

# Wrangler local state
.wrangler
.DS_Store
*.log

# Large local-only media (real content). Regenerate from source with ffmpeg — see README.
# The two small files in src/content/ ARE committed: primer.m4a is load-bearing (every
# follower loads it inside the unlock gesture) and screen.mp4 is the diagnostic clip.
public/media/

# Yarn 4 (node-modules linker)
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
